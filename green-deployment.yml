---
kind: Deployment
apiVersion: v1
metadata:
  name: green
  labels:
    app: green
    version: "1.01"
spec:
  replicas: 1
  selector:
    app: green
  template:
    metadata:
      labels:
        app: green
    spec:
      containers:
      - name: green
        image: awony/capstone
        ports:
        - name: http-server
          containerPort: 80
